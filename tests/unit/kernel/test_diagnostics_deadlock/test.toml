[test]
name = "test_diagnostics_deadlock"
source = "test_diagnostics_deadlock.cpp"
config = "test_diagnostics_deadlock_config.hpp"

[components]
features = ["sync"]

[link]
system_libraries = ["boost_context", "gtest", "gtest_main"]
