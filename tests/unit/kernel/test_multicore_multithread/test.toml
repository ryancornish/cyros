[test]
name   = "test_multicore_multithread"
source = "test_multicore_multithread.cpp"
config = "test_multicore_multithread_config.hpp"

[link]
# Uses its own main() — link gtest but not gtest_main.
system_libraries = ["boost_context", "gtest"]