[test]
name   = "test_singlecore_waitables"
source = "test_singlecore_waitables.cpp"
config = "test_singlecore_waitables_config.hpp"

[link]
# Uses its own main() — link gtest but not gtest_main.
system_libraries = ["boost_context", "gtest"]