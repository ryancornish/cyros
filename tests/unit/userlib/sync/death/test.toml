[test]
name = "test_sync_death"
layer = 7
kind = "unit"
source = "test_sync_death.cpp"
config = "test_sync_death_config.hpp"

# No port filter: the checks are kernel mutex code, and the panic path is
# common to both Linux ports.
[components]
features = ["sync"]

[link]
system_libraries = ["boost_context", "gtest", "gtest_main"]
