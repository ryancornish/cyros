name = "chrono"
description = "Time-flavoured blocking: alarm waitable, sleep_for/sleep_until"

dependencies = [
   "kernel",
   "time",
   "sync",
]

sources = [
   "alarm.cpp",
   "semaphore_timed.cpp",
]

public_headers = [
   "public_headers/alarm.hpp -> cyros/chrono/alarm.hpp",
   "public_headers/chrono.hpp -> cyros/chrono/chrono.hpp",
]
