[profile]
name = "unit_test_coverage"
toolchain = "../toolchains/gcc-coverage.toml"
# config_header is intentionally omitted — pass via -c/--config at the CLI

[layout]
source_root = "../../src"
output_root = "../../out"

[components]
port = "linux_boost"
time_driver = "simulation"

[features]
enable = []

[output]
archive = "libcortos.a"